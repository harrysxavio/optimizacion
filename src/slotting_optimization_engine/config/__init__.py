"""
config — Central configuration management.

Provides path resolution, environment-variable loading, and shared constants.
Centralising config prevents scattered hard-coded paths across modules and
simplifies environment-specific overrides (development, CI, production).
"""
